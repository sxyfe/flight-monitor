# OpenSpec 写作规范

- **所有 OpenSpec 产物必须使用简体中文撰写**，包括：`proposal.md`、`design.md`、`tasks.md`、`specs/**/*.md`。
- 需求描述使用 SHALL / MUST；场景使用 `#### Scenario:` + WHEN / THEN。
- 能力命名仍使用 kebab-case 英文标识（如 `nl-search-ui`），正文说明用中文。
